'use strict';
const $=s=>document.querySelector(s), app=$('#app'), overlay=$('#overlay');
const phases=['启蒙期','成长期','青春期'], powers=['安全力','脑波力','实感力','创心力','沟通力'];
const activities=[['未来守望者 · 城市站','上海 · 城市站'],['数智安全体验营','北京 · 国家会议中心']];
const activityConfigs=[
  {code:'WQTAY',mode:'快速成长模式',duration:3600,total:9,counts:[3,3,3],group:'活动指定卡牌组'},
  {code:'SAFEX',mode:'标准模式',duration:5000,total:15,counts:[5,5,5],group:'活动指定卡牌组'}
]; // Illustrative activity data, not production configuration.
const currentConfig=()=>activityConfigs[state.activity??0];
const totalCount=()=>state.counts.reduce((a,b)=>a+b,0);
const titles=['屏幕之外的约定','睡前的手机','危险的模仿','网络陌生人','游戏充值诱惑','朋友的误会','社交媒体发帖','考试前的压力','选择兴趣班','面对校园霸凌','手机使用时间','新同学的邀请'];
const state={screen:'login',tab:0,verified:false,agreed:false,phone:'',password:'',activity:null,ended:false,sessionId:null,mode:'quick',counts:[3,3,3],nav:0,card:null,choice:null,history:[],paused:false,remaining:3600,phase:0,filter:-1,page:0,side:-1};
const B01={code:'B01',title:'屏幕之外的约定',type:'身体安全',event:'晚饭一端上来，小伍先找的不是勺子，而是手机。动画一响，TA就一边盯着屏幕一边往嘴里塞饭，咀嚼和吞咽总跟着剧情忽快忽慢，连汤顺着嘴角流下来都没顾上擦。妈妈问“今天的鱼香不香”，小伍愣了一下，才发现自己根本说不出吃了什么，也分不清自己到底饿不饿、饱不饱。',options:['小伍继续边刷边吃，觉得“只要饭进了嘴，怎么吃都算吃了”。','小伍把手机推到一边，但还开着声音，想着“我先听着，也不算看”。','小伍先把手机交给大人，看着自己吃了哪些东西，感受肚子状态如何，夸夸大人做的美食。']};
function btn(text,action,cls='',disabled=false){return `<button data-action="${action}" class="${cls}" ${disabled?'disabled':''}>${text}</button>`}
function toast(s){$('#toast').textContent=s;$('#toast').style.display='block';clearTimeout(toast.t);toast.t=setTimeout(()=>$('#toast').style.display='none',3500)}
function closeButton(action='close'){return `<button class="close" data-action="${action}" aria-label="关闭">×</button>`}
function render(){if($('#toast'))$('#toast').style.display='none';app.innerHTML=state.screen==='login'?login():state.screen==='activity'?wizard(false):state.screen==='config'?wizard(true):game();inspectIds();}
function inspectIds(){document.querySelectorAll('button,input,select,section,header,h1,h2,nav,article').forEach((el,i)=>{if(!el.dataset.odId)el.dataset.odId=el.id||el.dataset.action?.replace(/:/g,'-')||`region-${state.screen}-${i}`})}
function login(){return `<div class="center"><section class="panel login" data-od-id="login-panel"><div class="login-art"><div class="eyebrow">WATCHER ACCESS</div><h1>回到你的<br>守望者席位</h1><p>登录后可继续桌游计分、现场监督与成长复盘。</p><img class="wu" src="assets/wu.webp" width="520" height="781" alt="官方小伍形象"><div class="permissions"><div><b>✓</b> 会话按设备安全保存</div><div><b>✓</b> 卡牌内容仅在本局按需读取</div><div><b>✓</b> 录音需要再次获得你的授权</div></div></div><div class="login-form">${closeButton('login-close')}<div class="eyebrow">《伍力全开》桌游计分系统</div><h1>守望者登录</h1><small>使用已授权的手机号进入 · 离线演示</small><div class="tabs" role="tablist">${btn('密码登录','tab:0',state.tab===0?'active':'')}${btn('验证码登录','tab:1',state.tab===1?'active':'')}</div><label class="field" for="phone">手机号</label><input id="phone" autocomplete="off" inputmode="numeric" maxlength="11" placeholder="请输入 11 位手机号" value="${state.phone}"><small>仅用于登录与账号安全验证</small><label class="field" for="password">${state.tab===0?'登录密码':'短信验证码'}</label><div class="password"><input id="password" type="${state.tab===0?'password':'text'}" maxlength="32" placeholder="${state.tab===0?'至少 6 位字符':'演示验证码：123456'}" value="${state.password}">${btn(state.tab===0?'显示':'获取','password-action')}</div><small>${state.tab===0?'至少 6 位字符':'短信发送仅作本地演示'}</small>${btn(`<span>${state.verified?'✓ 演示验证已完成':'待接阿里云验证码服务'}</span><span class="cyan">${state.verified?'已验证':'验证'}</span>`,'verify','verify')}<label class="agreement"><input id="agree" type="checkbox" ${state.agreed?'checked':''}>我已阅读并同意 ${btn('隐私说明','privacy','link')} 与 ${btn('使用条款','terms','link')}</label>${btn('进入计分系统　→','login','primary login-submit',!validLogin())}</div></section></div>`}
function validLogin(){return /^1\d{10}$/.test(state.phone)&& (state.tab===0?state.password.length>=6:state.password==='123456')&&state.verified&&state.agreed}
function wizard(config){return `<div class="center"><section class="panel wizard">${closeButton('wizard-close')}<header class="wizard-head"><div class="eyebrow">新游戏</div><h2>${config?'查看当前活动配置':'选择当前活动'} <span class="step">步骤 ${config?'2':'1'} / 2</span></h2><p>${config?'配置来自所选活动。本页只调整三个阶段的数量，进入游戏后再选择具体卡牌。':'直接选择现场正在开展的活动。一次开始并完成的游戏就是一局。'}</p></header><div class="wizard-body">${config?configBody():activityBody()}</div><footer class="footer">${btn(config?'上一步':'返回桌游模式',config?'back':'wizard-close')}${btn(config?'开始本局 →':'下一步 →',config?'start':'next','primary',config?!validCounts():state.activity===null)}</footer></section></div>`}
function activityBody(){return '<p class="muted">选择当前已有活动。以下为原型示例数据；活动码仅展示，无须输入。</p>'+activities.map((a,i)=>`<button class="activity ${state.activity===i?'selected':''}" data-action="activity:${i}" aria-pressed="${state.activity===i}"><span class="pin">⌖</span><span><strong>${a[0]}</strong><small>${a[1]} · 活动码 ${activityConfigs[i].code}</small></span><span class="open">● 当前开放</span><span>›</span></button>`).join('')}
function configBody(){const c=currentConfig(),a=activities[state.activity??0];return `<div class="selection"><div><h3>${a[0]}</h3><p class="muted">${a[1]} · ${c.code}</p></div><span class="step">活动预设</span></div><div class="preset"><span>游戏模式<b>${c.mode}</b></span><span>本局时长<b>${Math.floor(c.duration/60)} 分 ${c.duration%60} 秒</b></span><span>卡牌总数<b>${c.total} 张</b></span></div><p class="muted">${c.group} · 示例配置，正式值由活动提供</p><div class="section-label">三个阶段的卡牌数量</div><div class="counts">${phases.map((p,i)=>`<div class="count"><b>${p}</b><div class="stepper">${btn('−',`count:${i}:-1`,'',state.counts[i]<=0)}<output aria-label="${p}数量">${state.counts[i]}</output>${btn('+',`count:${i}:1`,'',totalCount()>=c.total)}</div></div>`).join('')}</div><div class="allocation" role="status">${validCounts()?'✓':'○'} 已分配 ${totalCount()} / ${c.total} 张${validCounts()?'':' · 请完成分配'}</div>`}
function validCounts(){return state.counts.every(n=>Number.isInteger(n)&&n>=0)&&totalCount()===currentConfig().total}
function game(){return productPage()}
function sideContent(i){return railPanel(i)}
function cardView(){let c=state.card;return `<article class="card"><div class="card-meta"><b>${c.code}</b><span>│　${c.type}</span>${btn('查看真实原卡','original')}</div><div class="scenario"><div class="cyan">先暂停，再看看发生了什么。</div><h1>${c.title}</h1><p>${c.event}</p></div><div class="options">${c.options.map((t,i)=>btn(`<span class="letter">${'ABC'[i]}</span><span>${t}</span>`,`choice:${i}`,`option ${state.choice===i?'selected':''}`,state.paused)).join('')}</div>${state.choice!==null?`<section class="feedback"><div class="feedback-head"><span class="letter">${'ABC'[state.choice]}</span><div><small class="cyan">本张卡牌反馈</small><br><strong>卡牌 ${c.code} · 选择 ${'ABC'[state.choice]}</strong></div><small style="margin-left:auto">讨论反馈 · 分值待核定</small></div><p>${['屏幕会分散对进食和身体感受的注意。可以先暂停，重新感受食物的味道与饱腹状态。','移开屏幕是一次尝试，持续播放的声音仍可能分散注意。一起讨论怎样给吃饭留出完整时间。','把注意力带回食物和身体，也向家人表达感谢。邀请玩家分享自己可以做到的小约定。'][state.choice]}</p><details><summary>查看伍力理由</summary>此处为原型讨论引导，不代表正式卡库评分。生产反馈与属性变化需由对应版本卡库提供。</details><div class="feedback-actions">${btn('听反馈','speak')}${btn('完成本张 · 选择下一张 →','finish','primary',state.paused)}</div></section>`:'<small>请选择 A / B / C，查看本张反馈。</small>'}</article>`}
function show(html,cls='dialog'){if(state.side!==-1){state.side=-1;render()}overlay.innerHTML=`<div class="overlay"><section class="panel ${cls}" role="dialog" aria-modal="true">${closeButton()}${html}</section></div>`;inspectIds();overlay.querySelector('button')?.focus()}
function picker(){let all=Array.from({length:36},(_,i)=>({code:`${['B','P','S'][state.phase]}${String(i+1).padStart(2,'0')}`,title:titles[i%12],power:i%5}));let filtered=all.filter(c=>state.filter===-1||c.power===state.filter);let pages=Math.ceil(filtered.length/12);state.page=Math.min(state.page,pages-1);show(`<h2>快速选择情景牌</h2><p>从下方选择一张情景牌，选择后将立即读取卡牌并进入游戏</p><div class="stage-tabs">${phases.map((p,i)=>btn(p,`phase:${i}`,state.phase===i?'active':'')).join('')}</div><div class="filters"><span>伍力筛选：</span>${['全部',...powers].map((p,i)=>btn(p,`filter:${i-1}`,state.filter===i-1?'active':'')).join('')}</div><div class="card-grid">${filtered.slice(state.page*12,state.page*12+12).map((c,i)=>btn(`<b>${c.code}</b><span>${c.title}</span>`,`pick:${c.code}`,`card-tile ${state.card?.code===c.code||(!state.card&&i===0)?'selected':''}`)).join('')}</div><div class="pagination">${Array.from({length:pages},(_,i)=>btn(`●<span class="sr-only" style="font-size:10px"> ${i+1}</span>`,`page:${i}`,i===state.page?'active':'')).join('')}</div><small>原型选卡目录 · B01 提供真实原卡内容，其余仅演示筛选与选择状态</small>`,'picker')}
function loadCard(code){if(!state.sessionId)return toast('请先开始新游戏');if(state.ended)return toast('本局已结束，请开启新游戏');if(state.paused)return toast('请先恢复游戏');code=code.replace(/^2026/,'').toUpperCase();if(!/^[BPS]\d{2}$/.test(code)||Number(code.slice(1))<1||Number(code.slice(1))>36)return toast('未找到卡牌，请输入 B01，或使用选卡目录');if(code!=='B01'){state.card={...B01,code,title:'卡牌内容待接入',type:'原型目录',event:'当前编号已选中。此编号的正式场景、选项与反馈尚未接入；请使用 B01 体验完整读卡流程。',options:[]};}else state.card={...B01};state.choice=null;overlay.innerHTML='';render()}
function save(){try{sessionStorage.setItem('wqt-prototype-session',JSON.stringify({sessionId:state.sessionId,activity:state.activity,history:state.history,remaining:state.remaining,counts:state.counts,ended:state.ended,attributes:state.attributes,audit:state.audit}));}catch{toast('浏览器未允许本地保存，当前会话仍可继续')}}
function timeText(){return `${String(Math.floor(state.remaining/60)).padStart(2,'0')}:${String(state.remaining%60).padStart(2,'0')}`}
setInterval(()=>{if(state.sessionId&&state.screen==='game'&&!state.paused&&!state.ended&&state.remaining>0){state.remaining--;document.querySelectorAll('[data-clock]').forEach(el=>el.textContent=timeText());if(state.remaining===0){state.paused=true;show('<h2>本局时间已到</h2><p>计时已停止，你可以查看本局复盘。</p>'+btn('查看复盘','end','primary'));}if(state.remaining%10===0)save()}},1000);
document.addEventListener('input',e=>{if(e.target.id==='phone'){state.phone=e.target.value.replace(/\D/g,'');e.target.value=state.phone}if(e.target.id==='password')state.password=e.target.value;if(e.target.id==='activity-code')e.target.value=e.target.value.replace(/[^a-z]/gi,'').toUpperCase();if(state.screen==='login')$('[data-action="login"]').disabled=!validLogin()});
document.addEventListener('change',e=>{if(e.target.id==='agree'){state.agreed=e.target.checked;$('[data-action="login"]').disabled=!validLogin()}if(e.target.id==='mode'){state.mode=e.target.value;render()}});
document.addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target.id==='card-code')loadCard(e.target.value);if(e.key==='Escape'){overlay.innerHTML='';const prev=state.side;state.side=-1;render();if(prev!==-1)document.querySelector('[data-action="side:'+prev+'"]')?.focus();}if(e.key==='Tab'&&overlay.firstChild){const f=[...overlay.querySelectorAll('button:not(:disabled),input,select,textarea,summary')];if(e.shiftKey&&document.activeElement===f[0]){e.preventDefault();f.at(-1).focus()}else if(!e.shiftKey&&document.activeElement===f.at(-1)){e.preventDefault();f[0].focus()}}});
document.addEventListener('click',e=>{const b=e.target.closest('[data-action]');if(!b||b.disabled)return;let [a,v,w]=b.dataset.action.split(':');if(a==='close'){overlay.innerHTML='';return}if(a==='tab'){state.tab=+v;state.password='';state.verified=false;render()}else if(a==='verify'){state.verified=true;render();toast('仅完成本地演示验证；阿里云验证码服务待接入')}else if(a==='password-action'){if(state.tab===0){$('#password').type=$('#password').type==='password'?'text':'password';b.textContent=$('#password').type==='password'?'显示':'隐藏'}else toast('演示验证码：123456；未发送真实短信')}else if(a==='privacy'||a==='terms'){show(`<h2>${a==='privacy'?'隐私说明':'使用条款'}</h2><p>本原型仅用于本地交互演示。不发送手机号、密码或卡牌记录，不启动录音。登录信息仅存于当前内存，刷新后清除。</p><p>正式服务的授权、数据保存期限与条款须由上线服务提供。</p>`)}else if(a==='login-close'){show('<h2>退出登录面板？</h2><p>此原型从守望者登录开始。关闭提示即可继续。</p>')}else if(a==='login'&&validLogin()){state.password='';state.screen='game';state.nav=0;render()}else if(a==='new-game'){if(activeSession())return toast('请先结束当前局');state.draftBackup={activity:state.activity,counts:[...state.counts],mode:state.mode};state.screen='activity';state.activity=null;state.side=-1;render()}else if(a==='activity'){state.activity=+v;state.counts=[...currentConfig().counts];state.mode=currentConfig().mode;render()}else if(a==='next'&&state.activity!==null){state.screen='config';render()}else if(a==='back'){state.screen='activity';render()}else if(a==='wizard-close'){if(state.draftBackup)Object.assign(state,state.draftBackup);overlay.innerHTML='';state.screen='game';state.nav=0;render()}else if(a==='logout'){overlay.innerHTML='';state.screen='login';state.verified=false;render()}else if(a==='count'){let i=+v,n=state.counts[i]+(+w);if(n>=0&&totalCount()+(+w)<=currentConfig().total){state.counts[i]=n;render()}}else if(a==='start'&&validCounts()){state.screen='game';state.remaining=currentConfig().duration;state.paused=false;state.ended=false;state.sessionId=crypto.randomUUID();state.attributes=[3,3,3,3,3];state.audit=[];state.skills=[false,false,false,false,false];state.side=-1;state.reviewOpen=false;state.history=[];state.observation='';state.card=null;state.choice=null;state.nav=0;state.phase=state.counts.findIndex(n=>n>0);save();render()}else if(a==='nav'){state.nav=+v;state.side=-1;state.reviewOpen=false;overlay.innerHTML='';render()}else if(a==='home'){state.screen='game';state.nav=0;render()}else if(a==='review-last'){state.screen='game';state.nav=0;state.reviewOpen=true;render()}else if(a==='review'){state.nav=0;state.reviewOpen=true;render()}else if(a==='side'){state.side=state.side===+v?-1:+v;render()}else if(a==='picker'){if(!activeSession()||state.paused)return toast('请先开始或恢复本局');state.side=-1;render();picker()}else if(a==='phase'){state.phase=+v;state.page=0;picker()}else if(a==='filter'){state.filter=+v;state.page=0;picker()}else if(a==='page'){state.page=+v;picker()}else if(a==='pick'){loadCard(v)}else if(a==='choice'){if(!state.paused&&!state.ended){state.choice=+v;render()}}else if(a==='finish'){if(state.choice===null||state.paused||state.ended||!state.card)return;if(!state.history.some(h=>h.code===state.card.code)){state.history.push({code:state.card.code,title:state.card.title,choice:state.choice,phase:state.phase});state.audit.push({text:'完成 '+state.card.code+' · 选择 '+'ABC'[state.choice],time:new Date().toLocaleTimeString()});save()}if(state.history.length>=currentConfig().total){state.ended=true;state.paused=true;state.reviewOpen=false;save()}else{state.card=null;state.choice=null}render()}else if(a==='pause'){if(state.ended)return;state.paused=!state.paused;render();toast(state.paused?'游戏已暂停，计时与答题已锁定':'游戏已恢复')}else if(a==='blocked-new'){toast('请先结束当前这一局，再开启新游戏')}else if(a==='stop'){show('<h2>停止本局并复盘？</h2><p>已完成 '+state.history.length+' 张卡牌。停止后将冻结计时，保留本局选择记录。</p><div class="actions">'+btn('继续游戏','close')+btn('确认停止','end','primary')+'</div>')}else if(a==='end'){state.ended=true;state.paused=true;state.nav=0;state.side=-1;state.reviewOpen=true;overlay.innerHTML='';save();render()}else if(a==='original'){if(state.card.code!=='B01')return toast('该编号原卡尚未接入');show('<h2>2026B01 · 官方原卡</h2><img class="original" src="assets/2026B01.webp" width="1089" height="612" alt="2026B01 风险事件与 A、B、C 决策选项">','dialog image-dialog')}else if(a==='speak'){if(!('speechSynthesis' in window))return toast('此浏览器不支持语音朗读');speechSynthesis.cancel();const u=new SpeechSynthesisUtterance($('.feedback p').textContent);u.lang='zh-CN';speechSynthesis.speak(u);toast('正在尝试使用设备中文语音朗读')}else if(a==='observation'){show('<h2>现场观察</h2><p>记录玩家表达与讨论，不录音。</p><textarea id="observation" rows="4" style="width:100%;background:#101b3b;color:white;padding:12px" aria-label="观察内容"></textarea><div class="actions">'+btn('保存观察','save-observation','primary')+'</div>')}else if(a==='save-observation'){state.observation=$('#observation').value;overlay.innerHTML='';toast('现场观察已保存在本次会话')}else if(a==='export'){const data=JSON.stringify({activity:activities[state.activity??0][0],history:state.history,observation:state.observation||'',notice:'离线原型记录，不包含正式分值'},null,2);const url=URL.createObjectURL(new Blob([data],{type:'application/json'}));const link=document.createElement('a');link.href=url;link.download='wqt-session.json';link.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}});
render();
