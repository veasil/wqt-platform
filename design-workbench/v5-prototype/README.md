# Astra / OpenDesign 原型 V5（待视觉验收）

双击 `standalone-prototype.html` 可离线打开，图片、CSS 和交互脚本已内嵌。源文件为 `index.html`、`styles.css`、`app.js`，图片见 `assets/`。

## 演示流程

1. 输入任意符合格式的手机号和至少 6 位密码，点击演示验证、勾选条款后登录。验证码登录演示码为 `123456`。
2. 活动码输入 `WQTAY`，校验并选择活动。
3. 配置共 9 张阶段卡牌，开始游戏。
4. 读卡、选择选项、查看反馈；可体验快速选卡、暂停及结束等原型交互。

没有真实登录、短信发送、阿里云验证码、计分持久化或后端连接。卡牌、活动与分值仅用于演示，不能视为生产业务规则。

## 交付状态

由 OpenDesign 中的 Local Codex / GPT-6 Astra 生成。原始运行与后续收尾均被 OpenDesign 后台关闭中断；本目录是对已保存源码的独立文件导出，不代表生成运行成功或视觉验收通过。

已知待修：配置页底部分配提示空间不足；读卡反馈页可能出现整页纵向滚动；快速选卡浮层位置、背景纹理仍需对照参考调整。八张参考批注未全部完成最终逐项验收。

只导出显式列出的源文件与图片，没有包含浏览器配置、缓存、账号信息或系统字体。未修改生产前端、认证或数据库。

重新导出：`node export.cjs "OpenDesign 项目绝对路径"`。
