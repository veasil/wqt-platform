# Astra / OpenDesign 原型 V5（待视觉验收）

双击 `standalone-prototype.html` 可离线打开，图片、CSS 和交互脚本已内嵌。源文件为 `index.html`、`styles.css`、`app.js`，本轮页面与交互在 `interface.js` / `interface.css`，图片见 `assets/`。

## 演示流程

1. 输入任意符合格式的手机号和至少 6 位密码，点击演示验证、勾选条款后登录。验证码登录演示码为 `123456`。
2. 登录后直接进入桌游模式，点击「新游戏」，直接选择已有活动。已删除额外工作台中间页。五位活动码仅展示，不作为进入门槛。
3. 查看活动携带的模式、时长和总卡数，只调整三个阶段的数量。示例活动分别携带 9 张 / 15 张预设；并非所有活动固定 9 张。具体卡牌在进入游戏后选择。
4. 读卡、选择选项、查看反馈；可体验快速选卡、暂停及结束等原型交互。

没有真实登录、短信发送、阿里云验证码、计分持久化或后端连接。卡牌、活动与分值仅用于演示，不能视为生产业务规则。

## 交付状态

由 OpenDesign 中的 Local Codex / GPT-6 Astra 生成。原始运行与后续收尾均被 OpenDesign 后台关闭中断；本目录是对已保存源码的独立文件导出，不代表生成运行成功或视觉验收通过。

2026-09-12：按已确认 DESIGN-SPEC D2 同步：仅桌游模式渲染 v4 三入口工具栏和互斥侧向面板；监督页提供同局雷达/五维调整/状态/历史/重置/导出；我的保留六项账号内部侧导航，补齐本地资料编辑、密码格式校验、示例学习、活动申请/查询/场次入口、文件空态及反馈。中间工作台已删除，登录及取消配置均回到桌游页。上述账号功能只作本地演示，不提交生产服务。

复盘不再用历史列表冒充：保留本局记录及既有链路的接入边界。参考当前仓库 `public/game-review.js` 的 openReview：读取本局 → 最低卡数检查 → 生成 → 上传（失败回退本地报告）→ 查看/导出。离线原型未接入该服务，不能生成正式报告。

仍待完成：真实活动和卡牌数据、正式计分/技能、复盘服务接入、阶段推进细则以及八张参考图的最终视觉验收。本轮仅修正已确认的业务框架，不代表整个产品实现完成。

只导出显式列出的源文件与图片，没有包含浏览器配置、缓存、账号信息或系统字体。未修改生产前端、认证或数据库。

重新导出当前修订：`node export.cjs`。不要再导入旧 OpenDesign 源码覆盖本轮修改。
