# 主页视觉样稿 01

直接打开 index.html，无外部依赖。V5 保持不变。

参考 https://variant.com/shared/0345d449-c584-4797-bc83-ceaac5474cc4 的三栏 3:6:3 比例、20px 列间距、32px 圆角和低对比 3px 边框。中文使用本机黑体栈，未嵌入参考页英文字体；品牌标记为临时文字标记。

仅供视觉评审：B01 内容为压缩演示文案，伍力数值和时间为固定展示数据。支持选项反馈切换与暂停锁定；其他入口明确提示未展开，不接后端，不改变计分与认证规则。

验证：1440×1000 截图；390px 窄屏无横向溢出；选择 A 后反馈改变；暂停后选择和完成按钮禁用；浏览器未报告脚本错误。尚未验证完整游戏流程，视觉需用户确认。

frontend-design 技能用于控制留白与层级，以用户提供的参考约束优先。后续再决定是否扩展登录、配置和选卡页。
