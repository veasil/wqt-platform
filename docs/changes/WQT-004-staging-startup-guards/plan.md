# WQT-004 · 执行计划

## 步骤

| 任务 | 执行者 | 状态 |
|---|---|---|
| 启动前环境校验、只读预检、运行配置复核、短信关闭回退 | Astra | 已实现，本地及PG16/18 CI通过 |
| 完整 schema 合成数据与回滚测试 | Luna staging_fixture，Astra复核 | 已实现，PG16/18 CI通过 |
| 纯启动配置测试 | Luna startup_guard_tests | 9/9本地通过 |
| PG16/18 CI及集成审核 | Astra | 代码版本436d3d3全部通过 |
| 生产失败部署只读调查、触发器调整方案 | Astra | 已调查；获批关闭生产Git触发器并复查运行状态 |
| 独立预发布资源与预算gate | 需求方 / Astra | 首月$9采购完成；机器READY、自动续费关闭；独立项目/PG18已创建 |
| 合成数据部署、真实服务联调、最终验收 | 团队 | 资源已就绪，待独立短信/OSS配置及应用部署 |

共享文件由主控维护，子代理仅新增分配的fixture或测试文件。所有测试指向helper创建登记的独占库，不运行旧bench，不读生产变量值。

## 回退

当前代码在隔离分支，可Git回退。新保护会拒绝缺少配置的旧部署方式；不得直接将其部署到现有生产服务。需先准备环境配置并确认部署触发器，不能为启动成功移除校验。合成seed仅用于明确的空开发/预发布库，调用方提供同一事务和密码；拒绝覆盖已有业务表。
