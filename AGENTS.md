# WQT 项目工程规范

本文件是 WQT 团队共享的项目规范，适用于所有贡献者和编码代理。个人偏好与个人辅导约定不属于项目知识。

## 项目与权威来源

当前系统为 Express / PostgreSQL 单体，托管桌游、组织管理和平台管理三个前端。代码位置见 [README](README.md)。

| 内容 | 权威制品 |
|---|---|
| 开发协作、自动循环、GitHub 流程 | [CONTRIBUTING.md](CONTRIBUTING.md) |
| vNext 范围、需求 ID、阶段门与发布责任 | [需求与验收基线](docs/vnext-requirements-and-acceptance.md) |
| 应用架构与实施顺序 | [架构方案](docs/wqt-application-architecture.md) |
| 租户、账号、游戏、数据约束 | [领域规则](docs/architecture/domain-rules.md) |
| 目录职责与迁移规范 | [目录规范](docs/architecture/repository-layout.md) |
| 开发环境与验证限制 | [开发指南](docs/development/local-development.md) |
| 每次开发的制品与自动检查 | [变更入口](docs/changes/README.md) |

版本化制品是团队交接依据。聊天中确认的需求和决定应同步到制品并记录来源；未提交的文件仍是工作稿。本地代码、目标设计和线上运行状态必须分别描述。

## Artifact-driven 工作原则

1. 先定位需求来源、范围、非目标和可验证的验收标准。需求提出方或指定产品负责人确认需求与标准；已有确认可复用，不重复审批。
2. 实现前形成与变更规模匹配的制品。小改动可在一份变更记录中包含需求、方案与证据；跨权限或数据边界的变更需明确设计与迁移影响。
3. 实现、测试、评审和修复在已确认范围内自动循环，直到验收标准有证据支持或出现具体阻塞。每轮更新制品，不以口头“完成”替代验证。
4. 执行者不能自行降低验收标准，也不能替代用户完成业务验收。范围或高后果边界变化时，记录差异与影响，由相应负责人确认后继续依赖部分。
5. 阻塞时记录失败证据、已尝试方法和所需输入；可继续不依赖该输入的工作。自动循环不意味着自动上线、无限重试或无人值守调度。
6. 代码、文档、测试与验收证据通过 GitHub 的分支和 PR 关联。自动化代码检查通过、技术评审通过、业务验收通过和发布完成是不同状态。

## 模型分工与任务委派

目标、任务状态和证据通过 spec/plan/evidence 支持跨代理与跨会话接续。阶段目标与实现方法可按证据调整并记录；需求方只验收最终目标和重要 gate，不审批日常派工与范围内修复。完整调度、升级和交接规则见[子代理工作流](docs/development/subagents-workflow.md)。

团队于 2026-09-12 确认采用 Astra 主控、Luna 执行的工作流：

- **Astra（`gpt-6-astra`）**：拟定验收标准、规划与拆分任务、处理复杂情况、审查集成结果并判断技术标准是否满足。需求与标准由需求方确认，产品最终验收仍由指定验收人完成。
- **Luna（`gpt-5.6-luna`）**：承接边界明确、可独立验证的重复性代码工作，例如按既定规则提取文件、批量修改调用点、补齐已定义用例。由 Astra 按实际需要拉起子代理。
- 委派必须给出关联制品、允许修改的文件、保持不变的行为、验收标准、验证命令和升级条件。并行任务不得同时修改同一文件；没有独立子任务时由主控直接完成。
- Luna 遇到需求歧义、接口契约变化、权限/数据边界变化、迁移风险或无法解释的失败时，先提交证据交 Astra 判断，不自行扩大范围。Astra 按团队决策权限处理，必要时交需求方确认。
- Luna 返回变更摘要、验证结果和未解决问题；Astra 复核 diff、集成影响与证据，再继续自动循环。子代理报告通过不等于集成验收通过。
- 主控模型由任务设置选择；文档不会自动切换当前模型。指定模型不可用时应说明限制，不静默用其他模型代替。

## 已确认的领域边界

- **Organization 就是 Tenant。** 沿用 `organizations.id`；`enterprise_id` 是兼容字段名，不建立重复租户实体。
- **组织场次永久归创建时的组织。** 人员转组织不转移历史场次归属；该规则已确认，WQT-002 已实现数据库和查询改造，隔离回归通过、尚未迁移生产。永久归属不定义数据保留期限。
- 离开原组织即失去该组织场次访问、历史未知不计组织统计和活动关联边界已由需求方在 G-MIGRATION 确认。多组织成员、代理跨租户授权、跨产品权益仍是独立待决事项，不通过重构隐式决定。

完整状态、迁移原则与实现证据见领域规则。已确认原则不重复索要授权；新增高后果决策不能伪装成目录整理。

## 工程规则

- 采用逐步模块化的单体。先明确业务责任，再按需要分离 HTTP、用例和数据访问；不为简单查询强制三层，不引入无实际需求的通用框架。
- 保持行为的重构保留 API、响应、中间件和路由顺序。修复授权或改变数据归属作为可审阅的独立行为变更。
- 认证与租户范围依赖后端实时校验，不能仅信前端组织 ID 或 JWT 中的旧信息。列表、详情、统计、写入、导出和外部调用均纳入范围验证。
- 数据迁移必须有存量分析、歧义处理、约束检查和回退设计。不能用当前成员关系伪造历史归属。
- 事务固定同一数据库连接；并发配额、业务码唯一性和重复提交通过明确机制与测试保障，不能只用“先查再写”推断正确。
- 测试使用明确隔离的数据库与外部依赖。服务启动会写库；旧 SQLite bench 尚未适配 PG，不能作为安全的现成验证入口。
- 不覆盖工作区中无关改动，不提交凭据、个人数据或本地产物；新增可重复测试必须纳入 Git。
- 以实际检查结果报告完成度。构建通过不等于权限、并发、恢复或产品验收通过。

## 团队知识维护

修改规则时同步相关制品，保留需求 ID、决定来源和实现状态。可复用的修复经验进入[历史记录](docs/engineering-history.md)，稳定规则归入领域文档。

迭代收尾记录：验收证据、改变的假设、新增约束、遗留风险、后续责任与文档变更。无新增风险时如实写明，不制造形式化工作。
